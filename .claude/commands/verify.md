---
description: Build, package, validate, and socket-test this clapp (npm run verify)
---
Run `npm run verify`. If it passes, say so briefly. If a step fails, read that step's
output (and `$TMPDIR/maps-verify.log` for the socket step), diagnose, and fix it —
then re-run.
